# Product system

This directory translates methodology into product decisions.

- `v2/` — current product source of truth. Start with `v2/product_learning_system_v2.md`.
- `v1/` — archived first product translation, kept for traceability.

Methodology source: `../learning_architecture/current/`.
Learning-experience layer: `../learning_experience/`.
Implementation reality: `../apps/` and `../packages/`.

The code remains frozen where v2 says `КОД ЗАМОРОЖЕН`: approve the decision log and validate the smallest integrated learning unit before broad implementation.
