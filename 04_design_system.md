# Intensive English — Дизайн-система и UX (MVP)

> Шаг 1 Discovery: проверка продуктовой логики + дизайн-система.
> Источник: скилл ui-ux-pro-max + ручная адаптация под аудиторию и двуязычность.

---

## 0. Важная поправка к авто-рекомендации

Скилл по слову «education» сначала предложил **детский стиль** (Claymorphism + шрифт Comic Neue, палитра «синий+жёлтый+розовый»). Для нашей аудитории — **подростки и взрослые профессионалы** (врачи, инженеры) — это не подходит. Берём **спокойный, премиальный, фокусирующий** тон. Ниже — адаптированная система.

---

## 1. Визуальный язык

- **Стиль:** мягкий минимализм + микровзаимодействия (тактильная отдача, лёгкие анимации 150–300 мс). Скруглённые формы, но сдержанно — не «конфетно». Много воздуха.
- **Принцип экрана:** одна задача — один экран — один главный CTA. Это и UX-стандарт, и прямое попадание в метод («снятие барьеров», ничего лишнего).
- **Режимы:** светлая + тёмная тема с самого начала (тёмная важна для долгих 5-часовых сессий — меньше устают глаза).

## 2. Шрифты (адаптировано под RU/EN)

⚠️ Авто-рекомендации (Poppins, Plus Jakarta Sans, Comic Neue) **плохо поддерживают кириллицу** — для двуязычного RU/EN интерфейса не годятся. Замена на шрифты с полной кириллицей:

- **Заголовки:** **Nunito** — тёплый, дружелюбный, но взрослый; полная кириллица.
- **Текст и интерфейс:** **Inter** — нейтральный, отличная читаемость, превосходная кириллица, идеален для карточек и чтения.
- Для длинных текстов скорочтения — Inter с увеличенным line-height (1.6–1.75).

```css
@import url('https://fonts.googleapis.com/css2?family=Nunito:wght@500;600;700;800&family=Inter:wght@400;500;600;700&display=swap');
/* heading: Nunito; body/ui: Inter */
```

## 3. Палитра — «Индиго» ✅ ВЫБРАНА (light)

Выбор Галины: вариант A — фокус, доверие, спокойствие; не детская, не «как у всех».

| Роль | Hex | Назначение |
|------|-----|-----------|
| Primary | `#4F46E5` | индиго — основной бренд, фокус и доверие |
| Primary dark | `#4338CA` | hover/нажатие, текст на soft-подложке |
| On Primary | `#FFFFFF` | текст на primary |
| Accent / CTA | `#FF6B5C` | тёплый коралл — энергия действия, кнопки «Начать» |
| Accent dark | `#E04E3F` | hover для CTA |
| Primary soft | `#EEF0FE` | подложки, пилюли, выделения |
| Success | `#16A34A` | прогресс, «слово усвоено» |
| Background | `#F5F5FB` | мягкий, почти белый с холодком |
| Foreground | `#1E1B4B` | основной текст (контраст AA+) |
| Muted text | `#6B7280` | вторичный текст |
| Border | `#ECECF5` | разделители |
| Destructive | `#DC2626` | ошибки |

Тёмная тема: фон `#14132B`, поверхности `#1E1B4B`/`#252245`, текст `#EAE9FB`, primary осветлить до `#8B83F0`, accent оставить тёплым `#FF6B5C`. Контраст проверять отдельно.

Реализована в [prototype/index.html](prototype/index.html) (палитра A).

## 4. Структура экранов (MVP)

```
Онбординг → Главная (Сегодня) → [Сессия по уровням] → Прогресс
```

1. **Онбординг (3 шага, progressive disclosure)**: цель → профессия/интересы → стартовый уровень. Коротко, без перегруза.
2. **Главная «Сегодня»**: цель дня, кнопка «Начать сессию», стрик, что на повторении. Один крупный CTA.
3. **Уровень 1 «Готовность»**: настрой, мягкая фраза-поддержка, опц. музыка, кнопка «Поехали».
4. **Флеш-словарь**: карточка слово+перевод+транскрипция+аудио; режимы «обычный» и «быстрый» (см. §6 — безопасность!).
5. **Скорочтение**: текст, таймер, перевод слова по тапу, в конце — замер скорости.
6. **Shadowing-плеер**: видео YouTube + субтитры + перевод; контролы темпа/повтора фрагмента.
7. **Повторения (SRS)**: очередь на сегодня, простое «знаю / не знаю».
8. **Прогресс**: слова усвоено, скорость чтения (график), стрик, рейтинг.

**Навигация:** нижняя панель (mobile) / боковая (desktop ≥1024px), ≤5 пунктов: Сегодня · Словарь · Чтение · Видео · Профиль.

## 5. Ключевые UX-паттерны
- Touch-таргеты ≥44px, отступы ≥8px.
- Состояния hover/press/disabled/focus — явные; focus-ring для клавиатуры.
- Загрузка >300 мс → скелетоны, не спиннер.
- Числа (таймеры, счётчики слов) — табличные цифры, без «прыжков».
- Пустые состояния с подсказкой и действием.
- Анимации: вход ease-out, выход быстрее; уважать `prefers-reduced-motion`.

## 6. 🔴 Безопасность быстрого предъявления (критично)

Метод предлагает показ карточек **0,1 сек** (10 показов/сек). Полноэкранное мигание с такой частотой может провоцировать **фотосенситивную реакцию/приступ** (WCAG 2.3.1: не более 3 вспышек/сек).

**Решение:**
- Меняется **только содержимое карточки** (слово), а не яркость всего экрана; фон стабильный, без резких скачков светимости/контраста — это снижает риск.
- **Дефолт — безопасная скорость** (≥0,3–0,5 сек); ускорение до 0,1 сек — **осознанное согласие** с предупреждением.
- Уважать `prefers-reduced-motion`; дать паузу/выход в любой момент.
- Предупреждение о фоточувствительности при первом запуске быстрого режима.

## 7. Критика продуктовой логики (что улучшить в ТЗ)

1. **Перевод по тапу vs метод.** Метод требует перевод РЯДОМ (параллельно), а в скорочтении мы дали «по тапу». Предлагаю: в shadowing — параллельные колонки (как в методе), в скорочтении — перевод по тапу + опция «показать весь перевод». Гибрид.
2. **Старт сразу с контекст-паков** усложняет MVP. Компромисс: общий частотный костяк + 1–2 готовых контекст-пака (напр. «саморазвитие», «медицина») на старте, остальное — по мере роста. Иначе MVP затянется.
3. **5 часов в день — риск выгорания в UI.** Нужны явные «релаксации» между блоками (метод это и предполагает: чередование активизации и релаксации). Заложить микро-паузы и смену активности в сессии.
4. **Озвучка слов.** Частотные базы не всегда имеют аудио ко всем словам. Нужен фолбэк (для редких слов — точечный TTS), иначе будут «немые» карточки. Отметить в ТЗ.
5. **Геймификация + «снятие барьеров».** Рейтинг может давить (противоречит «без стресса»). Решение: рейтинг — мягкий, опциональный; основной акцент на личный прогресс, а не сравнение.
6. **Двуязычие = двойной контент.** Все строки UI и подсказки — через i18n с первого дня, иначе переделка.

## 8. Anti-patterns (избегаем)
- Эмодзи вместо иконок → SVG (Lucide/Heroicons).
- Скучный «учебниковый» вид и отсутствие мотивации.
- Перегруженный онбординг.
- Жёсткое сравнение/наказание за ошибки (против духа метода).
- Шрифты без кириллицы.

## 9. Чек-лист перед вёрсткой
- [ ] Контраст текста ≥4.5:1 (обе темы)
- [ ] Touch ≥44px, focus-ring виден
- [ ] `prefers-reduced-motion` учтён
- [ ] Адаптив 375/768/1024/1440
- [ ] i18n RU/EN с первого экрана
- [ ] Предупреждение фоточувствительности для быстрого режима
- [ ] Кириллица во всех шрифтах
