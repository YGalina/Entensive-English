# Design exports

Downloaded standalone HTML and ZIP exports are preserved here as delivery artifacts. The structured Claude Design handoff in `../project/` is the design source to inspect; production UI lives in `../../../apps/`.
